name: api

on:
  push:
    paths: ["api/**", "packages/types/**", ".github/workflows/api.yml"]
  pull_request:
    paths: ["api/**", "packages/types/**", ".github/workflows/api.yml"]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: hardpull
          POSTGRES_DB: hardpull
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://postgres:hardpull@localhost:5432/hardpull
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: ci-test-secret
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @hardpull/types
        run: pnpm --filter @hardpull/types build

      - name: Run migrations
        run: pnpm --filter @hardpull/api exec node-pg-migrate up -m src/db/migrations

      - name: Typecheck
        run: pnpm --filter @hardpull/api exec tsc --noEmit -p tsconfig.json

      - name: Test
        run: pnpm --filter @hardpull/api test
