NEXT_PUBLIC_HARDPULL_API_URL=http://localhost:3001

# The CRE workflow's X25519 public key. Lender A seals its position records to this, so a wrong
# value produces ciphertext the enclave cannot decrypt. Print it with:
#   cd cre && go run ./cmd/keygen
NEXT_PUBLIC_CRE_WORKFLOW_PUBLIC_KEY_HEX=
